// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 🌟 BLOOM UI Quantum Interactions - Core Types
// ✨ STARWEAVE Universe Integration
// Reality Anchor: isdood
// Temporal Coordinate: 2025-06-02 17:15:13 UTC
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

const std = @import("std");
const bloom = @import("bloom");
const glimmer = @import("glimmer");
const starweave = @import("starweave");

// 💫 Core Configuration
pub const InteractionConfig = struct {
    reality_sync: f32 = 1.0,
    crystal_resonance: f32 = 1.0,
    quantum_flow: f32 = 1.0,
    starweave_binding: f32 = 1.0,
    glimmer_pattern: glimmer.Pattern = .crystal_flow,
};

// 🌱 Core State
pub const InteractionState = struct {
    active: bool = false,
    enabled: bool = true,
    reality_phase: f32 = 1.0,
    crystal_matrix: glimmer.Matrix = .{},
};

// 🌸 Core Position
pub const QuantumPosition = struct {
    x: f32,
    y: f32,
    z: f32 = 0.0,
    reality_plane: u8 = 1,
};

// ⭐ Core Dimensions
pub const QuantumDimensions = struct {
    width: f32,
    height: f32,
    depth: f32 = 0.0,
    reality_scale: f32 = 1.0,
};
